# הצטרפות לדשבורד הקבוצתי

## חלק 1 - מה שאתה עושה (מי שמצטרף)

3 שלבים, מאומתים בפועל (ככה זה עבד לשתי ריצות ראשונות שהצטרפו):

1. **פותחים חשבון חינמי** ב-[intervals.icu](https://intervals.icu) - מייל וסיסמה, שתי דקות.
2. **מחברים את השעון** - Settings ← Connections ← מוצאים את השעון שלכם (Garmin / Coros / Suunto / כל דבר אחר) ולוחצים להתחבר. חד-פעמי.
3. **מחכים לבקשה ממני (שחר) ולוחצים אישור (Yes)** כשהיא מגיעה.

זהו. ברגע שמאשרים, הריצות נכנסות לדשבורד לבד - גם ריצות עתידיות, בלי לעשות שום דבר נוסף.

⚠️ **חשוב:** אל תלחצו "Follow" על הפרופיל שלי בעצמכם - זה ייתן לכם גישה לראות אותי, הפוך ממה שרוצים. רק תחכו לבקשה שלי.

<details>
<summary>אם ריצה לא מופיעה אחרי כמה ימים (נדיר, לא קרה עד עכשיו)</summary>

אפשר להעלות ריצה בודדת ידנית כדי לבדוק שהחיבור תקין: ב-Garmin Connect פותחים את הריצה ← תפריט (⚙️) ← Export to TCX ← גוררים את הקובץ שירד ללוח השנה (Calendar) ב-intervals.icu, ליום המתאים.
</details>

---

## חלק 2 - מה שאני עושה (המארגן)

1. **למצוא את החבר באתר ולבקש לעקוב/לאמן** - מחפשים אותו בשורת החיפוש או ב"find athletes" בעמוד הפרופיל, נכנסים לפרופיל שלו, ולוחצים Follow או Coach.

2. **לחכות שהוא יאשר** - הוא יקבל התראה ויצטרך ללחוץ Yes.

3. **למצוא את המזהה (Athlete ID) שלו** - בטרמינל, בתיקיית `garmin-run-dashboard`:
   ```
   node fetch-data.js discover
   ```
   בפלט, מתחת ל-`/athletes`, אמור להופיע עכשיו יותר מרשומה אחת - שם וה-ID של החבר (נראה כמו `i123456`).

3. **להוסיף אותו ל-`config.json`** - בתוך מערך ה-`athletes`, שורה נוספת:
   ```json
   { "id": "i123456", "name": "השם שלו" }
   ```

4. **למשוך ולבדוק:**
   ```
   node fetch-data.js
   ```
   ולרענן את הדשבורד - הריצות של החבר אמורות להופיע.

**אם שלב 4 נכשל** (למשל שגיאת הרשאה על הפעילויות של החבר) - זה בדיוק הרגע שבו נגלה אם המפתח שלי כמאמן מספיק, או שצריך שהחבר ישלח לי גם את מפתח ה-API האישי שלו (מ-Settings ← Developer Settings אצלו). לא ידוע מראש - נבדוק בפועל.
